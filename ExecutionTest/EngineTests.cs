//-----------------------------------------------------------------------
// <copyright file="EngineTests.cs" company="Microsoft Corporation">
//     Copyright Microsoft Corporation. All rights reserved.
// </copyright>
//-----------------------------------------------------------------------

using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace ExecutionTest
{
    [TestClass]
    class EngineTests
    {
        #region Initialization

        // Make sure dte object is not null

        // Make sure command helper object is not null

        #endregion

        #region Site

        // Make sure site is connected

        #endregion

        //private Engine engine;

        [TestMethod]
        public void Constructor_InitializesEngine()
        {
            //int pid = 0;
            //string version = "hello";
            //var engine = new Engine(pid, version);
            //Assert.AreEqual(Path.Combine(Path.GetDirectoryName(Assembly.GetExecutingAssembly().Location), "Test.js"), this.fileNode.FullPath);
            //Assert.IsFalse(this.fileNode.IsEditable);
        }
    }
}
